Mercury is a logic programming language. The syntax and semantics is
similar to Prolog but unlike Prolog it is strongly typed and compiles
to efficient machine code.

This Slackbuild takes a while (> 1 hour) to build.
